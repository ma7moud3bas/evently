import { Meta, StoryObj } from "@storybook/react"
import ImageInput, { ImageInputProps } from "."

export default {
    title: "UI/ImageInput",
    component: ImageInput
} as Meta<ImageInputProps>

type Story = StoryObj<ImageInputProps>

export const imageInput: Story = {}